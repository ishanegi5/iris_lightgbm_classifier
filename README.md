# iris_lightgbm_classifier
Iris flower classification using LightGBM (multiclass classification). Achieved 100% accuracy with optimized hyperparameters. Includes model training, evaluation (accuracy, confusion matrix, classification report), and feature importance visualization.
